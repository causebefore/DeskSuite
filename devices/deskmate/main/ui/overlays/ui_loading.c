/*
 * 文件职责：实现 Loading 占位，后续创建加载中遮罩或进度控件。
 * 主要依赖：LVGL、UI 根容器。
 * 调用方：ui_main。
 */
#include "ui_loading.h"

esp_err_t ui_loading_init(void)
{
    return ESP_OK;
}

esp_err_t ui_loading_show(void)
{
    return ESP_OK;
}

esp_err_t ui_loading_hide(void)
{
    return ESP_OK;
}
