# 菜单标题保持 ASCII，兼容 esp-sr 在 Windows 上按系统代码页读取 sdkconfig。
menu "Communication - Firmware OTA"

    config FIRMWARE_OTA_CHECK_PATH
        string "OTA 检查端点"
        default "/api/v1/ota/check"
        help
            相对于设备服务基础地址的 OTA 检查端点。不同产品共用同一服务端时，
            应在项目级 sdkconfig.defaults 中覆盖为各自端点，避免取得其他产品固件。

endmenu
