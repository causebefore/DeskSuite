dependencies:
  espressif/esp_audio_codec:
    version: "2.5.0"
    rules:
      - if: target in [esp32s3]
  espressif/esp_audio_effects:
    version: "~1.2.1"
    rules:
      - if: target in [esp32s3]
